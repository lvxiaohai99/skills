# 模板目录

每个子目录是一套产出骨架。写文档前先读本表，再读选中目录里的 `功能需求.md`、`概要设计.md`。

| 包名 | 别名（用户也可说这些） | 何时用 |
| --- | --- | --- |
| `default` | 通用、默认 | **未指定模板时用这个** |
| `ivi` | 车载座舱、座舱、车机、IVI、DVR | 中控 / 仪表 / 行车记录仪等车机功能 |

自定义包：任意目录，内含同名两个文件即可；或分别指定两个 `.md` 路径。见 skill 正文「选定模板」。
