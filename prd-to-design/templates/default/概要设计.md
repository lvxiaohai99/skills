<!--
pack: default
填写规则：
- 保留 1～12 节标题。章节顺序就是建议阅读顺序，不要按 PRD 目录重排。
- 模块设计按开发依赖写（被依赖者在前），不要按产品功能清单顺序。
- {{占位符}} 换成实值。不适用的节写「本需求不适用」+ 原因。
- 每个设计点回指功能需求表里的 FR/NFR/OPEN。
落盘时删掉本注释。
-->

# 概要设计：{{产品名}}

> 依据：{{功能需求文件名}}　|　日期：{{日期}}
>
> 本文按开发依赖组织。需求变更先改功能需求表，再改本文对应 ID。

## 1. 文档信息

| 项 | 内容 |
| --- | --- |
| 需求基线 | FR {{n}} 条 / NFR {{n}} 条 / OPEN {{n}} 条 |
| 假设 | 技术栈、部署形态等 PRD 未写但设计采用的默认 |
| 参考 | {{来源文件}} |

## 2. 背景与目标

用 5～10 行写：问题、成功标准、范围内外。不复制 PRD 全文。

## 3. 系统上下文

- 参与者、外部系统、本系统边界
- 配一张 mermaid `flowchart`

## 4. 逻辑架构与模块依赖

按**被依赖者在上、依赖者在下**画模块图，并给出开发顺序（1、2、3…）。

## 5. 模块设计

对开发顺序中的每个模块：

### 5.x 模块名（建议开发顺序 = n）

- **职责：**
- **覆盖：** FR-… / NFR-…
- **依赖模块：**
- **对外接口：** 名称、方向、关键字段、错误
- **内部要点：** 状态、存储、线程/进程、关键算法（点到为止）
- **失败与降级：**

## 6. 核心流程

P0 主路径时序图（mermaid `sequenceDiagram`）。另列关键异常路径。

若有生命周期，补状态图（`stateDiagram-v2`）。

## 7. 数据概要

实体、主键/关键属性、来源与去向、留存与清理。不写完整 DDL，除非 PRD 已规定。

## 8. 接口概要

内外部 API / 总线 / 文件协议。每条映射到 FR。PRD 未定的标 OPEN。

## 9. 需求追溯

| 需求 ID | 模块 | 流程 / 接口 | 备注 |
| --- | --- | --- | --- |
| FR-… | | | |

P0 必须全部覆盖；NFR 写清落在哪一层。

## 10. 非功能如何落地

把 NFR 变成设计手段（队列、限速、超时、加密、降级），不要只复述指标。

## 11. 分期

| 阶段 | 包含的 ID | 可交付的闭环 | 依赖的 OPEN |
| --- | --- | --- | --- |

## 12. 风险

| 风险 | 相关 ID | 缓解 |
| --- | --- | --- |
