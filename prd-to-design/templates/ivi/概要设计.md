<!--
pack: ivi
适用：车机 / 座舱。模块顺序仍按开发依赖；运行域写进文档信息。
禁止把功能做到仪表域，除非 PRD 明确要求。
落盘时删掉本注释。
-->

# 概要设计：{{产品名}}

> 依据：{{功能需求文件名}}　|　日期：{{日期}}
>
> 本文按开发依赖组织。需求变更先改功能需求表，再改本文对应 ID。

## 1. 文档信息

| 项 | 内容 |
| --- | --- |
| 需求基线 | FR {{n}} 条 / NFR {{n}} 条 / OPEN {{n}} 条 |
| 运行域 | Android 中控 / Linux 仪表 / MCU（PRD 未写则假设并说明） |
| 假设 | 跟随现有座舱栈；信号/相机未定则桩 + OPEN |
| 参考 | {{来源文件}} |

## 2. 背景与目标

5～10 行。点明上电闭环与 App 体验分别是什么。不复制 PRD。

## 3. 系统上下文

参与者、摄像头、存储、车身信号、配置字、本系统边界。mermaid `flowchart`。明确**不进入**的域。

## 4. 逻辑架构与模块依赖

被依赖者在上。建议顺序典型为：配置门闩 → 存储/文件 → 录像或业务引擎 → 信号触发 → UI。按本需求裁剪。

## 5. 模块设计

每个模块除 default 那六项外，补一句**生命周期**（随谁启动、App 杀掉是否还在、下电是否丢状态）。

### 5.x 模块名（建议开发顺序 = n）

- **职责：**
- **覆盖：** FR-… / NFR-…
- **依赖模块：**
- **对外接口：**
- **内部要点：**
- **生命周期：**
- **失败与降级：**

## 6. 核心流程

P0 上电路径 + P0 用户路径（若不同）各一张时序图。补状态图（录/停/无介质/下电）。

## 7. 数据概要

文件命名、本地标记（停录是否 persist）、配置字。不写完整 DDL。

## 8. 接口概要

Camera / USB / 配置字 / 车身信号 / App↔服务。未定的标 OPEN，先桩。

## 9. 需求追溯

| 需求 ID | 模块 | 流程 / 接口 | 备注 |
| --- | --- | --- | --- |
| FR-… | | | |

P0 必须全部覆盖。

## 10. 非功能如何落地

NFR-LIFE、NFR-SAFE 必须落到进程优先级、启动方式、persist 范围，不要只复述。

## 11. 分期

| 阶段 | 包含的 ID | 可交付的闭环 | 依赖的 OPEN |
| --- | --- | --- | --- |

建议：先介质+主业务闭环，再 App 体验，再依赖实车信号的能力。

## 12. 风险

含：上电空窗、与 AVM/仪表抢相机、循环覆盖误伤用户文件、信号表为空仍宣称功能完成。
